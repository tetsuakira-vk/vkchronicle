---
layout: null
permalink: /bands.json
---
[{% for band in site.bands %}{"name":{{ band.name | jsonify }},"url":{{ band.url | jsonify }},"photo":{{ band.photo | default: "" | jsonify }},"tags":{{ band.tags | jsonify }}}{% unless forloop.last %},{% endunless %}{% endfor %}]
